import os
import csv
import re

skills = ['python', 'c++', 'python']
"""with open("base_skills1.csv", mode='w', newline='') as file:
    writer = csv.writer(file, delimiter=',', quotechar='|', quoting=csv.QUOTE_MINIMAL)
    writer.writerow(set(skills))
    file.close()"""

#text = " this is Prem singh rawat"
#print(re.findall(r'\b{}\b'.format('rem'), text, flags=re.IGNORECASE))
#print(list(set(skills)))

tu = ('hii', 'hello')
print(*tu)
# print(data)
